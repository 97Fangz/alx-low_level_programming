#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * description: 
 * Return: n if positive else return -n
 */

int _abs(int n)
{
if (n > 0)
	return (n);
else
	return (-n);
}
