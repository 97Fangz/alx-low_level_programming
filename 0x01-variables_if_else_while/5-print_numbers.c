#include<stdio.h>

/**
 * main -Entry Point
 * description: prints all single base 10 digits
 * Return: 0
 */

int main(void)
{
int btn;
for (btn = 0; btn < 10; btn++)
{
printf("%d", btn);
}
putchar('\n');
return (0);
}
