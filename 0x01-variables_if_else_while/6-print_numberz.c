#include<stdio.h>

/**
 * main - Entry point
 * description: prints all single digits of base 10
 * Return: 0
 */

int main(void)
{
	int btn;

	for (btn = 0; btn < 10; btn++)
	{
		putchar(btn);
	}
	putchar('\n');
	return (0);
}
